/**
 * Фикстура для tests/audit-2026-09-11-symbol-plus-coordinate.
 *
 * Здесь связка «имя + координата» НАМЕРЕННО разорвана переносом строки —
 * ровно та форма, которую построчная проверка круга 24 не видела. Файл
 * существует только затем, чтобы сторож доказал, что видит её теперь:
 * `stopMac`
 * (lib/mac-bridge.ts:373) — и ничем больше он не занят.
 *
 * Сам сторож сюда не заходит: `walk` пропускает каталог `fixtures` целиком,
 * иначе этот образец считался бы дефектом и чинить его пришлось бы вечно.
 */
export const SYMBOL_COORD_WRAPPED_FIXTURE = true;
