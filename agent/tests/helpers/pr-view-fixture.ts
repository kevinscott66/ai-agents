/**
 * Личность автора PR для фейковых ответов `gh pr view` в тестах.
 *
 * Аудит 2026-09-11, круг 50: `validatePrChecklist` запрашивает и проверяет
 * `author` / `isCrossRepository` — форк и незнакомый автор не рассматриваются
 * вовсе. Фикстуры без этих полей означают «gh ответил не тем, о чём просили»,
 * и гейт честно отвечает skip, то есть каждый тест мержа мерил бы не ту ветку.
 *
 * Отдельный файл, а не константа в каждом: логин доверенного автора — один
 * факт, и повторить его двадцатью строками значит завести двадцать мест,
 * которые разойдутся, когда список доверенных сменится. Здесь же видно, что
 * это именно «обычный свой PR», а не случайный набор полей.
 */
export const TRUSTED_PR_IDENTITY = {
  author: { login: "kevinscott66" },
  isCrossRepository: false,
} as const;

/** Тот же PR, но из чужого форка: личность, на которой гейт обязан остановиться. */
export const FORK_PR_IDENTITY = {
  author: { login: "stranger" },
  isCrossRepository: true,
} as const;
