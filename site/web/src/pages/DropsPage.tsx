import { DropsSection } from "../sections/DropsSection";

export function DropsPage(_: { path?: string }) {
  return (
    <main class="container page-section">
      <DropsSection />
    </main>
  );
}
