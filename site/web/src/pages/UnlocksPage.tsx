import { UnlocksSection } from "../sections/UnlocksSection";

export function UnlocksPage(_: { path?: string }) {
  return (
    <main class="container page-section">
      <UnlocksSection />
    </main>
  );
}
