# Снято с зелёного прогона 2026-09-11 на ветке
# claude/nightly-test-isolation-55b2cc (cd agent && bun test tests →
# 7838 pass / 39 skip / 0 fail).
# Прежние 7509 сняты в тот же день, до сторожей круга 51: границы скраба
# секретов в agent_actions и tasks, неучтённых 403 в miniapp-server,
# непроверяемой ссылки на CLAUDE.md и разбора нового поведения
# tools/sync-io-audit.ts. Набор вырос на 329 проверок и пробил потолок 7809 —
# ровно тот случай, ради которого коридор двусторонний.
# Обновление файла и есть та ручная работа, которую двусторонний коридор
# делает заметной (шапка scripts/assert-test-baseline.sh). Число снято с
# настоящего прогона в тот же день — ровно так, как требует та же шапка
# после красного потолка 27–29.08.
7838
